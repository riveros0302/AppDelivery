import { StyleSheet } from "react-native";

export const MyColors = {
  primary: "#f4991a",
  secondary: "#AAAAAA",
  button: "#ffd200",
};

export const MyStyles = StyleSheet.create({
  loading: {
    position: "absolute",
    bottom: 0,
    top: 0,
    right: 0,
    left: 0,
  },
});
